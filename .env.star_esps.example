# Star (ESPixelStick / sACN / Art-Net) — example
#
# This agent streams procedural patterns to a non-WLED pixel controller.
# Configure your ESPixelStick to receive either:
# - sACN (E1.31) unicast, or
# - Art-Net

CONTROLLER_KIND=pixel

# Required for pixel controllers
PIXEL_PROTOCOL=e131   # e131 or artnet
PIXEL_HOST=172.16.200.60
PIXEL_COUNT=50
PIXEL_UNIVERSE_START=1
PIXEL_CHANNELS_PER_UNIVERSE=510
PIXEL_PORT=5568
PIXEL_PRIORITY=100
PIXEL_SOURCE_NAME=Star ESP

# Identity
AGENT_ID=star_esps
AGENT_NAME=Star (ESPixelStick)
AGENT_ROLE=prop

# Recommended: shared key required for /v1/a2a/* and /v1/fleet/* (set the same on all agents)
A2A_API_KEY=

# Optional: local login (JWT) for this agent
AUTH_ENABLED=false
AUTH_USERNAME=admin
AUTH_PASSWORD=
AUTH_JWT_SECRET=
AUTH_JWT_TTL_S=43200
AUTH_COOKIE_SECURE=false
AUTH_TOTP_ENABLED=false
AUTH_TOTP_SECRET=
AUTH_TOTP_ISSUER=wled-show-agent

# FPS controls reuse the existing DDP vars (just affects pattern streaming rate)
DDP_FPS_DEFAULT=20
DDP_FPS_MAX=45

# Brightness cap (applies to streaming too)
WLED_MAX_BRI=180

DATA_DIR=/data

# Database (required; shared across fleet)
DATABASE_URL=mysql://wsa:wsa@db:3306/wsa
