services:
  ui:
    build: ./agent/ui
    ports:
      - "8088:80"
    depends_on:
      - api
    restart: unless-stopped

  api:
    build: ./agent
    env_file:
      - .env
    volumes:
      - ./data:/data
    expose:
      - "8088"
    restart: unless-stopped
